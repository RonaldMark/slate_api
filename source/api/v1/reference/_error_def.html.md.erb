#Error definition

###Duplicate reference.

Returned when you have sent the same reference twice.

> {
    "response_code": 400,
    "errors": [
        {
            "code": "0003",
            "message": "Transactions with the transaction_ref exist, request not saved"
        }
    ]
}